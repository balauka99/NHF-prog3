package object;

import loaders.SheetLoader;

public class Heart extends OsObject{
	public Heart() {
		name = "heart";
		sheet = new SheetLoader(, x, hitboxDefaultY, hitboxDefaultX, defaultTileSize)
	}
}
