package object;

import loaders.SheetLoader;

public class Heart extends OsObject{
	public Heart() {
		name = "heart";
		sheet = new SheetLoader("/objects/healt_sheet.png", x, hitboxDefaultY, hitboxDefaultX, defaultTileSize)
	}
}
